{
  "name": "@s4kit/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@s4kit/shared": "workspace:*",
    "hono": "^4.11.1",
    "ky": "^1.14.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
